<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/ic_background">

    <!-- Тулбар -->
    <androidx.appcompat.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="#22000000">

        <ImageButton
            android:id="@+id/btn_back_quest"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_background"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Назад" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="🔍 Янтарный Квест"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:gravity="center" />

        <TextView
            android:id="@+id/tv_sparks"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="✨ 10 Искр"
            android:textColor="#FFD700"
            android:textSize="14sp"
            android:paddingEnd="8dp" />
    </androidx.appcompat.widget.Toolbar>

    <!-- Прогресс и локация -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp"
        android:background="#CC000000">

        <TextView
            android:id="@+id/tv_quest_location_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFD700"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_quest_progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#CCCCCC"
            android:textSize="13sp"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- Прокручиваемая область с заданием -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Номер задания -->
            <TextView
                android:id="@+id/tv_task_number"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Задание 1/5"
                android:textColor="#3390FF"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginBottom="8dp" />

            <!-- Текст загадки -->
            <androidx.cardview.widget.CardView
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cardBackgroundColor="#CC000000"
                app:cardCornerRadius="12dp"
                app:cardElevation="4dp"
                android:layout_marginBottom="16dp">

                <TextView
                    android:id="@+id/tv_task_text"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:padding="16dp"
                    android:textColor="@android:color/white"
                    android:textSize="15sp"
                    android:lineSpacingMultiplier="1.4" />
            </androidx.cardview.widget.CardView>

            <!-- Ввод текста/числа -->
            <LinearLayout
                android:id="@+id/layout_input"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone">

                <EditText
                    android:id="@+id/et_answer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="Введи ответ"
                    android:textColor="@android:color/white"
                    android:textColorHint="#888888"
                    android:backgroundTint="#3390FF"
                    android:padding="12dp"
                    android:inputType="text" />

                <Button
                    android:id="@+id/btn_check"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Проверить"
                    android:textColor="@android:color/white"
                    android:backgroundTint="#3390FF"
                    android:layout_marginTop="8dp" />
            </LinearLayout>

            <!-- Варианты ответов -->
            <LinearLayout
                android:id="@+id/layout_choices"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone" />

            <!-- Кнопка фото -->
            <Button
                android:id="@+id/btn_photo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="📷 Сделать фото"
                android:textColor="@android:color/white"
                android:backgroundTint="#4CAF50"
                android:visibility="gone" />

            <!-- Обратная связь -->
            <TextView
                android:id="@+id/tv_feedback"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="15sp"
                android:textStyle="bold"
                android:layout_marginTop="12dp"
                android:visibility="gone" />

            <!-- Кнопка следующего задания -->
            <Button
                android:id="@+id/btn_next_task"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Следующее задание →"
                android:textColor="@android:color/white"
                android:backgroundTint="#3390FF"
                android:layout_marginTop="8dp"
                android:visibility="gone" />

            <!-- Кнопка следующей локации -->
            <Button
                android:id="@+id/btn_next_location"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="🔓 Осколок получен! Продолжить →"
                android:textColor="@android:color/white"
                android:backgroundTint="#FF8C00"
                android:textStyle="bold"
                android:layout_marginTop="8dp"
                android:visibility="gone" />
        </LinearLayout>
    </ScrollView>

    <!-- Кнопка подсказки внизу -->
    <Button
        android:id="@+id/btn_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="💡 Подсказка (1 ✨)"
        android:textColor="@android:color/white"
        android:backgroundTint="#555555"
        android:layout_margin="8dp" />

</LinearLayout>
